Optional 60-second mp4 files named `{sample_id}.mp4`.

Times in the file must start at 0 s = gold clip start (not YouTube absolute time).
If this folder is empty, the annotator uses the YouTube video_id instead.
