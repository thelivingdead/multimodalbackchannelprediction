# Shake axis audit (GOLD DEV only)

Traces are EMOCA Euler `rotation_xyz` (x, y, z). Geometric **shake** on these
clips is **y** (yaw-like). Geometric **nod** is **x**. Locked TEST rule used
**z** and was not rewritten. New TRAIN pseudo-labels (`results/shake/v2/`)
rank on **y**.

Videos were not on this Mac. Narrative:
`results/shake/v2/axis_audit/axis_audit.md`.
