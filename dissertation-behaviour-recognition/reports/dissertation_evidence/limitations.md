# Limitations

- If Stage A used `make_pilot_clips.py`, clips are synthetic. Do not report those F1 numbers as RealTalk.
- No human inter-annotator agreement yet (single annotator).
- TEST set does not exist yet.
- Eyebrow and lean are unsupported until EMOCA keys are verified.
- Rule detector proposes candidates; humans only accept/reject. Missed nods require using `a` to add.
