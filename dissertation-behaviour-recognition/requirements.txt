# Lightweight CPU pipeline: pose rule, stored CNN artefacts, figures, pytest.
# VideoMAE: also pip install -r requirements-video.txt
# Audio DEV: also pip install -r requirements-audio.txt
numpy>=1.24,<2.1
scipy>=1.10,<1.14
pandas>=2.0
matplotlib>=3.7,<3.10
scikit-learn>=1.3
pyyaml>=6.0
pillow>=10.0
pytest>=7.0
